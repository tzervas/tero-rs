[package]
name = "mycelium-std-iter"
version = "0.0.0"
description = "std.iter — iterator / fold combinators (total where the kernel guarantees it)."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
mycelium-core = { path = "../mycelium-core" }
mycelium-std-core = { path = "../mycelium-std-core" }
