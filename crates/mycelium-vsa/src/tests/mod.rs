//! In-crate unit tests (white-box), one submodule per extracted source module — the repo's
//! test-layout rule (logic files carry no test code; extraction happens as-touched, M-797).
//! The remaining modules' inline tests migrate here as they are next modified.

mod fhrr;
