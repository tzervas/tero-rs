# mycelium-proj.toml — the project manifest (the default source headers inherit from).
[project]
name        = "geometry"
kind        = "phylum"
version     = "1.2.0"
license     = "Apache-2.0"
authors     = ["Tyler Zervas"]
since       = "2026-01-10"
summary     = "2D/3D geometry primitives and certified swaps."
repository  = "https://github.com/example/geometry"
keywords    = ["geometry", "linear-algebra"]
lang        = "mycelium-0"

[surface]
exports     = ["geometry.shapes", "geometry.transform"]

[dependencies]
numerics    = { phylum = "numerics", version = "^2", hash = "blake3:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef" }
