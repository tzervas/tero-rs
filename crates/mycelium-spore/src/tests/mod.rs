//! In-crate test modules for `mycelium-spore` (CLAUDE.md test-layout rule).
//! White-box access via `use crate::…::*`; logic files carry no `#[cfg(test)]` inline code.

mod lib_tests;
mod registry_tests;
mod remote_tests;
