[package]
name = "mycelium-std-numerics"
version = "0.0.0"
description = "std.numerics — the honest ε/δ carrier (Approx<T>) and meet-composition surface over the ADR-010 numerics kernels (M-512)."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
mycelium-core = { path = "../mycelium-core" }
mycelium-std-core = { path = "../mycelium-std-core" }
mycelium-numerics = { path = "../mycelium-numerics" }

[dev-dependencies]
serde_json.workspace = true
